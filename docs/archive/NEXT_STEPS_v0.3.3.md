## v0.3.3 Goals
1) Privacy-verify: full AST dataflow (replace grep heuristic), rule whitelist, sinks map.
2) Trust chain: real signing keys + SBOM enrichment; policy exceptions report.
3) Live SLO baseline: 500+ live samples; budgets per route; sentinel tuning.
4) Windows runner (optional) & artifact size budgets.
5) Docs: Math annex + Ajv rule catalogue.
