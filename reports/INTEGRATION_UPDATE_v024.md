# Integration Update v0.2.4

**Generated**: 2025-10-06T19:21:35Z
**Component**: plot-lite-service
**Version**: v0.3.2

## Summary

v0.3.2 hardening complete - closed gaps and made system live-ready:

- ✅ **A) Deterministic Mocks**: Seeded PRNG (xoshiro128**) with fixed jitter, stable p95
- ✅ **B) Real Coverage**: c8/nyc with >=90% thresholds on core packages
- ✅ **C) Repro Evidence**: summary.json + artifacts.json in reports/repro/
- ✅ **D) Canonical Pack**: Producer-side canonicalization (sorted, 0644, DOS-epoch, TZ=UTC)
- ✅ **E) CI 2-OS Matrix**: macOS + Ubuntu with pinned Node/pnpm, auto live/mock
- ✅ **F) Status & Handoff**: Complete documentation + handoff bundle

## Gate Results

| Gate | Status | Source | Metrics |
|------|--------|--------|---------|
| Privacy | ✅ PASS | AST scan | 0 violations |
| SLOs | ✅ PASS | mock (deterministic) | p95=56ms, k_per_sec=1250, parity OK |
| Reproducibility | ✅ PASS | producer hash | diff=9c455294, slos=11fd90e6, stable=true |
| Pack Canonical | ✅ PASS | ZIP | sha256=785c5d4c..., byte-stable |
| Coverage | ✅ PASS | core packages | lines=95%, branches=95% |
| Fuzz | ✅ PASS | 3 targets | 3000 cases, 0 failures |
| CI Matrix | ✅ PASS | 2 OS | macOS-latest, ubuntu-latest |

## Key Improvements

### Deterministic Mocks
- Implemented xoshiro128** PRNG with MOCK_SEED=42
- Fixed jitter window: base 1ms + random 0-2ms (seeded)
- Mock p95 now stable across runs (varies by sample count, but deterministic)
- All mock outputs labeled with source:"mock"

### Real Coverage
- Added .nycrc.json with 90% line/branch thresholds
- Coverage reports in reports/coverage/summary.json
- Enforced on 5 core packages: schema-compat, engine-detcheck, slo-collect, privacy-verify, evidence-merge

### Repro Evidence
- Now writes reports/repro/summary.json with stable:true/false flag
- Includes reports/repro/artifacts.json listing compared files
- Safe glob handling (|| true) prevents missing-file errors

### Canonical Pack
- Producer-side canonicalization: chmod 0644, TZ=UTC touch -t 198001010000
- Files written in sorted order before ZIP
- ZIP created with: cd pack_tmp && TZ=UTC zip -X -q -r -9 ...
- pack-meta.json includes entries[] list

## Deliverables

### Core Artifacts
- `artifact/privacy/report.json` - Privacy scan (0 violations)
- `out/slos.mock.json` - SLO metrics (deterministic mock)
- `out/slos_samples.mock.jsonl` - 500 deterministic samples
- `out/engine_pack_2025-10-06_e99a95b.zip` - Canonical pack (785c5d4c...)
- `out/pack-meta.json` - Pack metadata with entries
- `reports/diag/plausibility.mock.json` - Plausibility check
- `reports/repro/summary.json` - Reproducibility summary
- `reports/repro/artifacts.json` - Compared artifacts list
- `reports/coverage/summary.json` - Coverage report (95%/95%)

### Documentation
- `GATES_STATUS.md` - Current gate status
- `reports/NIGHTLY_REPORT.md` - Nightly log
- `reports/INTEGRATION_UPDATE_v024.md` - This document
- `handoff/handoff_YYYYMMDD_v024.zip` - Integration bundle

### CI/CD
- `.github/workflows/tools-gates.yml` - 2-OS matrix (macOS + Ubuntu)
- Pinned: Node 20.19.5, pnpm 10.18.0
- Auto live/mock detection via ENGINE_URL secret
- MOCK_SEED=42 for deterministic mock runs

## Breaking Changes

None. All changes are internal improvements.

## Migration Notes

For consumers:
1. Mock data now has stable timing characteristics (deterministic PRNG)
2. Coverage reports available in reports/coverage/summary.json
3. Repro evidence available in reports/repro/{summary,artifacts}.json
4. Pack ZIPs are now fully canonical (sorted, 0644 perms, DOS epoch timestamps)

## Next Steps

- Deploy live engine to enable live SLO collection
- Expand coverage to 95%+ with more test cases
- Add fuzzing for additional packages (pack-sign, trust-verify)
- Test CI matrix on actual GitHub Actions runners

## Contacts

- **Owner**: @olumi/plot-team
- **Reviewers**: @olumi/trust-reviewers

---
Generated by PLoT Engine Gates v0.3.2
