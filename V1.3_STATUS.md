# UI-Impact Bundle v1.3 - Implementation Status

## Objective
Ship additive engine features for richer UI with zero breaking changes, preserved determinism, and robust tests.

## ✅ Completed (Part 1)

### Infrastructure & Utilities
- **src/util/quantiles.ts**: Compute bands (p10/p50/p90) from Monte Carlo samples
- **src/util/explainability.ts**: Top contributors + key paths for "Why" panel
- **src/util/canonical-json.ts**: Updated stampResponseHash() to exclude v1.3 additive fields

### Feature 3: Teaching Validator ✅
**File:** `src/routes/v1/validate.ts`

**New Suggestions:**
- `MISSING_BELIEF_ON_OUTCOME_EDGE`: "Add belief 0..1 to edges into outcomes to calibrate uncertainty."
- `WEIGHT_MAGNITUDE_HIGH`: "Consider rescaling weight to |weight| ≤ 3 for numerical stability."
- `DECISION_NO_OUTGOING`: "Connect decision node to options or outcomes."
- `OPTION_NO_OUTGOING`: "Connect option node to outcomes to model consequences."

**Status:** 200 OK with warnings + suggestions

### Feature 4: Limits & Health Chips ✅
**File:** `src/routes/v1/limits.ts`

**New Fields:**
- `max_nodes`: 50
- `max_edges`: 200
- `engine_p95_ms_budget`: 600

**Health Endpoint:** `/v1/health` already has:
- `engine_p95_ms_rolling`
- `last_compute_ms`
- `json_429_count`
- `sse_429_count`

### Determinism Guarantee ✅
**Updated:** `stampResponseHash()` excludes:
- `debug` (existing)
- `bands` (new)
- `compare` (new)
- `explainability` (new)
- `results.bands` (new)
- `results.compare` (new)

**Result:** `model_card.response_hash` unchanged for same inputs

---

## 🚧 In Progress

### Feature 1: Result Bands + Simple Compares
**Status:** Utilities ready, integration pending

**TODO:**
- Integrate `computeBands()` into `/v1/run`
- Add `results.bands` when distributions computed
- Add `results.compare` when comparing options
- Mirror in SSE `COMPLETE` event

### Feature 2: Explainability Atoms
**Status:** Utilities ready, integration pending

**TODO:**
- Integrate `computeExplainability()` into `/v1/run`
- Add `explainability` object (top_contributors, key_paths)
- Omit for graphs > 12 nodes
- Mirror in SSE `COMPLETE` event

### Feature 5: Template Catalog Gallery
**Status:** Not started

**TODO:**
- Update `/v1/templates/catalog` with:
  - `title`, `summary`, `node_counts`, `tags`, `version`
- Compute `node_counts` from template graphs

---

## 📋 Remaining Tasks

### Tests (Priority)
1. `results.bands.compare.contract.test.ts`
2. `explainability.atoms.contract.test.ts`
3. `validate.suggestions.test.ts`
4. `limits.health.surface.test.ts`
5. `templates.catalog.gallery.test.ts`
6. `hash.projection.stability.test.ts`

### OpenAPI Updates
- Extend `/v1/run` schema with `results.bands`, `results.compare`, `explainability`
- Extend SSE `COMPLETE` schema
- Extend `/v1/validate` with `suggestion` field
- Extend `/v1/limits` and `/v1/health` schemas
- Extend `/v1/templates/catalog` schema
- Add examples for all new fields

### Documentation
- Update UI handoff with field names and usage
- Add change-log for v1.3

---

## Guardrails Status

| Guardrail | Status |
|-----------|--------|
| Backward compatibility | ✅ Only additive fields |
| Determinism | ✅ Hash excludes new fields |
| Performance budget (p95 ≤ 600ms) | ⏳ To verify |
| Security (CORS, limits, no logs) | ✅ Unchanged |
| Docs & contracts | ⏳ In progress |

---

## Next Steps

1. **Integrate bands/compare into /v1/run** (~30 min)
2. **Integrate explainability into /v1/run** (~20 min)
3. **Update template catalog** (~15 min)
4. **Create all contract tests** (~60 min)
5. **Update OpenAPI schemas** (~30 min)
6. **Run verification & perf probe** (~15 min)

**Estimated Total:** ~3 hours

---

**Current Status:** Infrastructure complete, integration in progress
**Build:** ✅ Passing
**Tests:** Pending
